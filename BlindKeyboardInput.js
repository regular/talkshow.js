// Generated by CoffeeScript 1.3.3
(function() {
  var BlindKeyboardInput,
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  BlindKeyboardInput = (function() {

    function BlindKeyboardInput(delegate) {
      this.keyHandler = __bind(this.keyHandler, this);

      var _this = this;
      this.delegate = delegate;
      this.rowCount = $("#grid tr").length;
      this.colCount = $("#grid tr:eq(1) td").length;
      $(window).keyup(function(e) {
        return _this.keyHandler(e);
      });
      this.setFocusPosition(0, 0);
    }

    BlindKeyboardInput.prototype.isInMenu = function(e) {
      return this.focusPosition().left === 0;
    };

    BlindKeyboardInput.prototype.playNavigationSound = function() {
      return $(".keyboardFocus audio").each(function() {
        return this.play();
      });
    };

    BlindKeyboardInput.prototype.keyHandler = function(e) {
      switch (String.fromCharCode(e.keyCode)) {
        case 'J':
          if (this.isInMenu()) {
            this.setFocusPosition(0, 0);
            this.playNavigationSound();
          } else {
            this.enter();
          }
          break;
        case 'N':
          if (this.isInMenu()) {
            this.setFocusPosition(0, 1);
            this.playNavigationSound();
          } else {
            this.pop();
          }
          break;
        case 'M':
          if (this.isInMenu()) {
            this.setFocusPosition(1, 0);
            this.playNavigationSound();
            this.startTimer();
          } else {
            this.setFocusPosition(0, 0);
            this.stopTimer();
          }
          break;
        default:
          switch (e.keyCode) {
            case 37:
              this.splitStep(-1);
              break;
            case 39:
              this.splitStep(1);
              break;
            case 38:
              this.move(0, -1);
              break;
            case 40:
              this.move(0, 1);
              break;
            case 13:
              this.enter();
              break;
            case 32:
              break;
            default:
              return true;
          }
      }
      return false;
    };

    BlindKeyboardInput.prototype.startTimer = function() {
      var timerCallback,
        _this = this;
      timerCallback = function() {
        _this.timeoutID = window.setTimeout(timerCallback, 1000);
        _this.splitStep(1);
        return _this.playNavigationSound();
      };
      return this.timeoutID = window.setTimeout(timerCallback, 1000);
    };

    BlindKeyboardInput.prototype.stopTimer = function() {
      return window.clearTimeout(this.timeoutID);
    };

    BlindKeyboardInput.prototype.enter = function() {
      var focusPos, _ref;
      this.stopTimer();
      focusPos = this.focusPosition();
      if (focusPos != null) {
        if ((_ref = this.delegate) != null) {
          _ref.enterCell(focusPos.left, focusPos.top);
        }
        this.setFocusPosition(1, 0);
        this.playNavigationSound();
        return this.startTimer();
      }
    };

    BlindKeyboardInput.prototype.pop = function() {
      var _ref;
      this.stopTimer();
      if ((_ref = this.delegate) != null) {
        _ref.pop();
      }
      this.setFocusPosition(1, 0);
      this.playNavigationSound();
      return this.startTimer();
    };

    BlindKeyboardInput.prototype.focusPosition = function() {
      var cell, row, x, y;
      if ($(".keyboardFocus").length === 1) {
        cell = $(".keyboardFocus");
        row = cell.closest("tr");
        x = cell.index();
        y = row.index();
        return {
          left: x,
          top: y
        };
      } else {
        return null;
      }
    };

    BlindKeyboardInput.prototype.setFocusPosition = function(x, y) {
      $("td").removeClass("keyboardFocus");
      return $("#grid tr").eq(y).find("td").eq(x).addClass("keyboardFocus");
    };

    BlindKeyboardInput.prototype.move = function(dx, dy) {
      var cell, cells, focusPos, row, rows, x, y;
      if ($(".keyboardFocus").length === 0) {
        rows = $("#grid tr");
        row = rows.eq(Math.floor(rows.length / 2));
        cells = row.find("td");
        cell = cells.eq(Math.floor(cells.length / 2));
        return cell.addClass("keyboardFocus");
      } else {
        focusPos = this.focusPosition();
        x = focusPos.left;
        y = focusPos.top;
        x += dx;
        y += dy;
        if (x >= this.colCount) {
          x = 0;
        }
        if (x < 0) {
          x = this.colCount - 1;
        }
        if (y >= this.rowCount) {
          y = 0;
        }
        if (y < 0) {
          y = this.rowCount - 1;
        }
        return this.setFocusPosition(x, y);
      }
    };

    BlindKeyboardInput.prototype.step = function(d, sx, sy, ex, ey) {
      var cell, cells, focusPos, row, rows, x, y;
      if (sx == null) {
        sx = 0;
      }
      if (sy == null) {
        sy = 0;
      }
      if (ex == null) {
        ex = this.colCount - 1;
      }
      if (ey == null) {
        ey = this.rowCount - 1;
      }
      if ($(".keyboardFocus").length === 0) {
        rows = $("#grid tr");
        row = rows.eq(sy);
        cells = row.find("td");
        cell = cells.eq(sx);
        return cell.addClass("keyboardFocus");
      } else {
        focusPos = this.focusPosition();
        x = focusPos.left;
        y = focusPos.top;
        x += d;
        if (x < sx) {
          x = ex;
          y--;
        }
        if (x > ex) {
          x = sx;
          y++;
        }
        if (y < sy) {
          y = ey;
          x = ex;
        }
        if (y > ey) {
          x = sx;
          y = sy;
        }
        return this.setFocusPosition(x, y);
      }
    };

    BlindKeyboardInput.prototype.splitStep = function(d) {
      if (!this.isInMenu()) {
        return this.step(d, 1, 0, this.colCount - 1, this.rowCount - 1);
      } else {
        return this.step(d, 0, 0, 0, this.rowCount - 1);
      }
    };

    return BlindKeyboardInput;

  })();

  window.BlindKeyboardInput = BlindKeyboardInput;

}).call(this);
